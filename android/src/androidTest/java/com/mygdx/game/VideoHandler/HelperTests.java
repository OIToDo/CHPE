package com.mygdx.game.VideoHandler;

import android.graphics.Color;

public  class HelperTests {

    /*
    The current unit tests only work on API 29 and above.
     */

    // Returns the red value as int
    static int getRed(int red) {
        return Color.red(red);
    }

    // Returns the blue value as int
    static int getBlue(int blue) {
        return Color.blue(blue);
    }

    // Returns the green value as int
    static int getGreen(int green) {
        return Color.green(green);
    }
}
